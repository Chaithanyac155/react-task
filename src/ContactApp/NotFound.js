import React from "react";

function NotFound() {
  return (
    <h4
      style={{
        display: "flex",
        justifyContent: "center",
        height: "100vh",
        alignItems: "center",
        textAlign: "center",
      }}
    >
      404 Error: URL Not Found
    </h4>
  );
}

export default NotFound;
